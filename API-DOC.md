# Jibli Backend API Documentation

**API version:** v1  
**Base URL:** `/api/v1`  
**Content types:** `application/json` unless an endpoint explicitly requires `multipart/form-data`  
**Last updated:** 2026-09-07

This document describes the HTTP API currently implemented by the Jibli backend. Response examples show the application-level fields; MongoDB documents may also contain `_id`, `createdAt`, and `updatedAt` fields unless the endpoint applies a projection.

## Contents

- [Getting started](#getting-started)
- [Authentication](#authentication)
- [Common conventions](#common-conventions)
- [Data shapes](#data-shapes)
- [Authentication endpoints](#authentication-endpoints)
- [Restaurant endpoints](#restaurant-endpoints)
- [Product endpoints](#product-endpoints)
- [Customer endpoints](#customer-endpoints)
- [Cart endpoints](#cart-endpoints)
- [Order endpoints](#order-endpoints)
- [Static uploads](#static-uploads)
- [HTTP status codes and errors](#http-status-codes-and-errors)
- [Implementation notes](#implementation-notes)

## Getting started

### Start the server

The development server listens on the value of `PORT`, or port `3000` when it is not set.

### Required configuration

The application expects environment variables for the database, JWT signing, Cloudinary, and SMS provider. The relevant names are:

- `PORT`
- `MONGO_URI`
- `JWT_SECRET`
- `CLOUDINARY_CLOUD_NAME`
- `CLOUDINARY_API_KEY`
- `CLOUDINARY_API_SECRET`
- `HTTP_SMS_API_URL`
- `HTTP_SMS_API_KEY`
- `HTTP_SMS_PHONE_NUMBER`

### Example request

```http
POST /api/v1/auth/send-otp HTTP/1.1
Host: api.example.com
Content-Type: application/json

{"phone":"0101234567"}
```

## Authentication

Protected endpoints require a JWT in the `Authorization` header:

```http
Authorization: Bearer <jwt>
```

JWTs generated by the application contain an `id` and a `role` and are configured to expire after 30 days.

Supported roles currently used by route middleware are:

- `customer`
- `restaurant`

The OTP validator also accepts `rider`, but rider verification is not implemented and should not be used.

### Access notation

- **Public** — no JWT required.
- **Authenticated** — a valid JWT is required.
- **Customer** — a valid JWT with `role: "customer"` is required.
- **Restaurant** — a valid JWT with `role: "restaurant"` is required.
- **Restaurant owner** — restaurant authentication plus ownership of the route resource is required.

## Common conventions

### Coordinates

GeoJSON coordinates use `[longitude, latitude]`, not `[latitude, longitude]`:

```json
{
  "type": "Point",
  "coordinates": [35.9106, 31.9539]
}
```

### IDs

MongoDB identifiers are represented as 24-character hexadecimal strings, for example `665f1c9d2c8e4d0012345678`.

### Multipart uploads

The upload middleware stores files in memory and limits each file to **5 MB**. Images are uploaded to Cloudinary by the application.

### Security note

Only send sensitive fields over HTTPS. Do not log or expose JWTs, OTP values, SMS provider credentials, or API keys.

## Data shapes

The following shapes describe the principal request and response objects.

### Restaurant

```json
{
  "_id": "665f1c9d2c8e4d0012345678",
  "name": "Jibli Kitchen",
  "description": "Local food and fast meals",
  "phone": "0101234567",
  "email": "hello@example.com",
  "logo": { "url": "https://cdn.example/logo.png", "publicId": "jibli/logo" },
  "coverPhoto": {
    "url": "https://cdn.example/cover.png",
    "publicId": "jibli/cover"
  },
  "isOpen": true,
  "isActive": true,
  "tags": ["restaurant"],
  "rating": 4.5,
  "ratingCount": 120,
  "location": {
    "city": "Amman",
    "coordinates": { "type": "Point", "coordinates": [35.9106, 31.9539] }
  },
  "role": "restaurant",
  "workingDays": {
    "sunday": { "open": "09:00", "close": "23:00" }
  }
}
```

Restaurant tags are `fast-food`, `restaurant`, `traditional`, `healthy`, `pizzeria`, `desserts`, `barbcue`, and `sandwiches`.

### Product

```json
{
  "_id": "665f1c9d2c8e4d0012345679",
  "restaurantId": "665f1c9d2c8e4d0012345678",
  "name": "Chicken Sandwich",
  "category": "sandwich",
  "variants": [{ "size": "regular", "price": 3.5 }],
  "preparationTime": 15,
  "image": {
    "url": "https://cdn.example/product.png",
    "publicId": "jibli/products/product"
  },
  "description": "Grilled chicken sandwich",
  "isAvailable": true
}
```

Product categories are `sandwich`, `burger`, `pizza`, `taco`, `dishes`, `dessert`, `drinks`, and `other`.

### Location and saved address

```json
{
  "label": "Home",
  "city": "Amman",
  "coordinates": {
    "type": "Point",
    "coordinates": [35.9106, 31.9539]
  }
}
```

### Cart item

```json
{
  "productId": "665f1c9d2c8e4d0012345679",
  "quantity": 2,
  "variant": { "size": "regular", "price": 3.5 },
  "unitPrice": 3.5,
  "totalPrice": 7
}
```

### Order status

Valid order statuses are `pending`, `accepted`, `preparing`, `ready-to-pickup`, `out-for-delivery`, `at-door`, `delivered`, and `cancelled`.

## Authentication endpoints

### Send OTP

`POST /api/v1/auth/send-otp`

**Access:** Public  
**Rate limit:** 3 requests per IP/phone combination per 15 minutes.

Sends a six-digit verification code by SMS. The code is valid for five minutes.

#### Request body

| Field   | Type   | Required | Description                            |
| ------- | ------ | :------: | -------------------------------------- |
| `phone` | string |   Yes    | Phone number sent to the SMS provider. |

```json
{ "phone": "0101234567" }
```

#### Success response — `201 Created`

```json
{ "message": "OTP created successfully." }
```

#### Possible responses

- `400 Bad Request` — `{ "message": "Phone is required." }`
- `429 Too Many Requests` — `{ "status": "error", "message": "Too many OTP requests. Please try again later." }`
- `500 Internal Server Error` — SMS provider, database, or configuration failure.

### Verify OTP

`POST /api/v1/auth/verify-otp`

**Access:** Public  
**Rate limit:** 5 requests per IP/phone combination per 5 minutes.

Verifies the SMS code and, for an existing account, returns a JWT.

#### Request body

| Field   | Type   | Required | Allowed values                                 |
| ------- | ------ | :------: | ---------------------------------------------- |
| `phone` | string |   Yes    | Ten digits according to the current validator. |
| `code`  | string |   Yes    | At least six characters.                       |
| `role`  | string |   Yes    | `customer`, `restaurant`, or `rider`.          |

```json
{
  "phone": "0101234567",
  "code": "123456",
  "role": "customer"
}
```

#### Existing customer — `200 OK`

```json
{
  "message": "OTP verified successfully. Existing customer.",
  "isNewCustomer": false,
  "token": "<jwt>"
}
```

#### New customer — `200 OK`

```json
{
  "message": "OTP verified successfully. New customer.",
  "isNewCustomer": true
}
```

Restaurant verification uses the corresponding `isNewRestaurant` field and returns a token for an existing restaurant.

#### Possible responses

- `400 Bad Request` — `{ "message": "Phone, code and role are required." }`
- `429 Too Many Requests` — verification rate limit exceeded.
- `500 Internal Server Error` — `OTP not found`, invalid/expired OTP, validation failure, or database failure as currently propagated.

## Restaurant endpoints

Base path: `/api/v1/restaurant`

### Restaurant login

`POST /api/v1/restaurant/login`

**Access:** Public

#### Request body

```json
{ "phone": "0101234567" }
```

| Field   | Type   | Required |
| ------- | ------ | :------: |
| `phone` | string |   Yes    |

#### Success response — `200 OK`

```json
{
  "restaurant": { "_id": "665f1c9d2c8e4d0012345678", "name": "Jibli Kitchen" },
  "token": "<jwt>",
  "message": "Restaurant logged in successfully"
}
```

#### Errors

- `400` — `{ "message": "Phone number is required" }`
- `500` — `Restaurant does not exist`, `Restaurant not found`, or a database error.

### Create restaurant

`POST /api/v1/restaurant/create`

**Access:** Public

#### Request body

```json
{
  "name": "Jibli Kitchen",
  "phone": "0101234567",
  "email": "hello@example.com",
  "description": "Local food and fast meals",
  "tags": ["restaurant", "fast-food"],
  "location": {
    "city": "Amman",
    "coordinates": { "type": "Point", "coordinates": [35.9106, 31.9539] }
  },
  "isOpen": true,
  "isActive": true,
  "workingDays": {
    "sunday": { "open": "09:00", "close": "23:00" }
  }
}
```

Required fields are `name`, `phone`, `tags`, and `location`. The complete `workingDays` object, when supplied, contains `sunday` through `saturday`, each with `open` and `close` in `HH:mm` format. Optional fields include `email`, `description`, `logo`, `coverPhoto`, `rating`, `ratingCount`, `isOpen`, `isActive`, and `workingDays`.

#### Success response — `201 Created`

```json
{
  "restaurant": { "_id": "665f1c9d2c8e4d0012345678", "name": "Jibli Kitchen" },
  "token": "<jwt>",
  "message": "Restaurant created successfully"
}
```

#### Errors

- `400` — `{ "message": "Request body is empty" }`
- `500` — duplicate phone, validation, or database error.

### Get products by category

`GET /api/v1/restaurant/:restaurantId/products?category=<category>`

**Access:** Public

#### Parameters

| Location | Name           | Type   | Required |
| -------- | -------------- | ------ | :------: |
| path     | `restaurantId` | string |   Yes    |
| query    | `category`     | string |   Yes    |

#### Success response — `200 OK`

```json
{
  "products": [],
  "message": "Products fetched successfully"
}
```

#### Errors

- `400` — `{ "message": "restaurantId and category are required" }`
- `500` — restaurant not found, invalid ID, or database error.

### Update restaurant status

`PUT /api/v1/restaurant/:restaurantId/status`

**Access:** Restaurant owner

#### Request body

```json
{ "status": true }
```

| Field    | Type    | Required | Description                                |
| -------- | ------- | :------: | ------------------------------------------ |
| `status` | boolean |   Yes    | Updates the restaurant's `isActive` field. |

#### Success response — `200 OK`

```json
{
  "restaurant": { "_id": "665f1c9d2c8e4d0012345678", "isActive": true },
  "message": "Restaurant status updated successfully"
}
```

#### Errors

- `400` — missing `restaurantId` or `status`.
- `401` — missing or invalid JWT.
- `403` — token is not a restaurant token or does not own the route resource.
- `500` — restaurant not found or database error.

### Update restaurant settings

`PUT /api/v1/restaurant/:restaurantId/settings`

**Access:** Restaurant owner  
**Content type:** `multipart/form-data`

#### Form fields

Restaurant settings may be supplied as form fields, including `name`, `description`, `phone`, `email`, `isOpen`, `isActive`, `tags`, and location/working-day fields. Current implementation passes body fields through to the update service; clients should send only supported restaurant fields.

#### Files

| Field        | Type       | Max count | Limit |
| ------------ | ---------- | :-------: | ----: |
| `logo`       | image file |     1     |  5 MB |
| `coverPhoto` | image file |     1     |  5 MB |

#### Success response — `200 OK`

```json
{
  "restaurant": { "_id": "665f1c9d2c8e4d0012345678" },
  "message": "Restaurant settings updated successfully"
}
```

#### Errors

- `400` — `{ "message": "restaurantId is required" }`
- `401` — missing or invalid JWT.
- `403` — wrong role or another restaurant's ID.
- `500` — upload, Cloudinary, validation, or database error.

### List restaurants

`GET /api/v1/restaurant/all`

**Access:** Public

#### Success response — `200 OK`

```json
{
  "restaurants": [],
  "message": "Restaurants fetched successfully"
}
```

The public projection excludes `phone`, `ratingCount`, `createdAt`, `updatedAt`, and `role`.

### Search restaurants

`GET /api/v1/restaurant/search?name=<name>`

**Access:** Public

#### Query parameters

| Name   | Type   | Required |
| ------ | ------ | :------: |
| `name` | string |   Yes    |

#### Success response — `200 OK`

```json
{
  "restaurants": [],
  "message": "Restaurants fetched successfully"
}
```

The same public projection as the list endpoint is returned.

#### Errors

- `400` — `{ "message": "name is required" }`
- `500` — database or search error.

### Get restaurant by ID

`GET /api/v1/restaurant/:restaurantId`

**Access:** Public

#### Success response — `200 OK`

```json
{
  "restaurant": { "_id": "665f1c9d2c8e4d0012345678", "name": "Jibli Kitchen" },
  "message": "Restaurant fetched successfully"
}
```

The public projection excludes `phone`, `ratingCount`, `createdAt`, `updatedAt`, and `role`. A missing restaurant currently results in `restaurant: null` rather than a dedicated `404` response.

### Update restaurant order status

`PUT /api/v1/restaurant/orders/:orderId/status`

**Access:** Restaurant JWT

#### Request body

```json
{
  "status": "preparing",
  "preparationTime": 20
}
```

| Field             | Type   | Required | Allowed values               |
| ----------------- | ------ | :------: | ---------------------------- |
| `status`          | string |   Yes    | Any valid order status.      |
| `preparationTime` | number |    No    | Preparation time in minutes. |

#### Success response — `200 OK`

```json
{
  "order": { "_id": "665f1c9d2c8e4d0012345680", "status": "preparing" },
  "message": "Order status updated successfully"
}
```

The authenticated restaurant ID is used to scope the update.

## Product endpoints

Products are mounted directly under `/api/v1`; there is no `/api/v1/product` prefix.

### Create product

`POST /api/v1/products`

**Access:** Restaurant JWT  
**Content type:** `multipart/form-data`

#### Form fields

| Field             | Type               | Required | Description                                                              |
| ----------------- | ------------------ | :------: | ------------------------------------------------------------------------ |
| `restaurantId`    | string             |   Yes    | Restaurant MongoDB ID.                                                   |
| `name`            | string             |   Yes    | At least 4 characters.                                                   |
| `category`        | string             |   Yes    | One of the product categories.                                           |
| `variants`        | JSON string        |   Yes    | JSON array of `{ "size": string, "price": number }`.                     |
| `preparationTime` | number string      |   Yes    | Minutes.                                                                 |
| `isAvailable`     | `"true"`/`"false"` |   Yes    | Availability flag.                                                       |
| `description`     | string             |    No    | Maximum 200 characters.                                                  |
| `price`           | number string      |    No    | Parsed by the controller but not persisted as a top-level product field. |

#### File

| Field   | Type       | Required | Limit |
| ------- | ---------- | :------: | ----: |
| `image` | image file |   Yes    |  5 MB |

Example `variants` value:

```json
[
  { "size": "regular", "price": 3.5 },
  { "size": "large", "price": 5 }
]
```

#### Success response — `201 Created`

```json
{
  "message": "Product created successfully",
  "product": { "_id": "665f1c9d2c8e4d0012345679", "name": "Chicken Sandwich" }
}
```

#### Errors

- `400` — missing product data or image.
- `401` — missing or invalid JWT.
- `403` — token is not a restaurant token.
- `500` — malformed `variants`, Cloudinary, validation, duplicate product, or database error.

### List products

`GET /api/v1/products`

**Access:** Public

#### Success response — `200 OK`

```json
{
  "message": "Products retrieved successfully",
  "products": []
}
```

Timestamps and `__v` are excluded from the product projection.

### Get product by ID

`GET /api/v1/products/:productId`

**Access:** Public

#### Success response — `200 OK`

```json
{
  "message": "Product retrieved successfully",
  "product": {
    "_id": "665f1c9d2c8e4d0012345679",
    "name": "Chicken Sandwich",
    "category": "sandwich",
    "variants": [{ "size": "regular", "price": 3.5 }],
    "preparationTime": 15,
    "isAvailable": true,
    "image": { "url": "https://cdn.example/product.png" },
    "restaurantId": "665f1c9d2c8e4d0012345678"
  }
}
```

#### Errors

- `400` — `{ "message": "Product ID is required" }`
- `500` — invalid product ID or product does not exist.

### Update product

`PUT /api/v1/products/:productId`

**Access:** Restaurant owner  
**Content type:** `multipart/form-data` (JSON-compatible body fields are also parsed by Express when no file is supplied)

#### Form fields

All product fields are optional for the update request. Common fields are `name`, `category`, `variants`, `preparationTime`, `description`, and `isAvailable`. `isAvailable` must be sent as `"true"` or `"false"` in multipart form data.

#### File

| Field   | Type       | Max count | Limit |
| ------- | ---------- | :-------: | ----: |
| `image` | image file |     1     |  5 MB |

#### Success response — `200 OK`

```json
{
  "message": "product has been updated successfully",
  "product": { "_id": "665f1c9d2c8e4d0012345679" }
}
```

#### Errors

- `401` — missing or invalid JWT.
- `403` — wrong role or product does not belong to the authenticated restaurant.
- `404` — product not found during ownership authorization.
- `500` — upload, validation, Cloudinary, or database error.

### Delete product

`DELETE /api/v1/products/:productId`

**Access:** Restaurant owner

#### Success response — `200 OK`

```json
{ "message": "Product deleted successfully" }
```

#### Errors

- `401` — missing or invalid JWT.
- `403` — wrong role or product ownership failure.
- `404` — product not found during authorization.
- `500` — product does not exist or database error.

## Customer endpoints

Base path: `/api/v1/customer`

### Register customer

`POST /api/v1/customer/register`

**Access:** Public

#### Request body

```json
{
  "fullName": "Alex Example",
  "phone": "0101234567",
  "email": "alex@example.com",
  "location": {
    "city": "Amman",
    "coordinates": { "type": "Point", "coordinates": [35.9106, 31.9539] }
  },
  "savedRestaurants": [],
  "savedAddresses": []
}
```

Required fields are `fullName`, `phone`, and `location`. The role defaults to `customer`; a supplied role must be `customer`.

#### Success response — `200 OK`

```json
{
  "customer": { "_id": "665f1c9d2c8e4d0012345681", "role": "customer" },
  "token": "<jwt>",
  "message": "Customer created successfully"
}
```

#### Errors

- `400` — `customer data is required` when the body is absent.
- `500` — duplicate phone, validation, or database error.

### Get customer profile

`GET /api/v1/customer/profile`

**Access:** Authenticated JWT. The route currently does not enforce `role: "customer"`; the JWT subject is used as the customer ID.

#### Success response — `200 OK`

```json
{
  "customer": { "_id": "665f1c9d2c8e4d0012345681", "fullName": "Alex Example" },
  "message": "Customer profile fetched successfully"
}
```

#### Errors

- `400` — `customer id is required`.
- `401` — missing or invalid JWT.
- `500` — customer not found or database error.

### Add saved address

`POST /api/v1/customer/saved-address`

**Access:** Customer JWT

#### Request body

```json
{
  "label": "Home",
  "city": "Amman",
  "coordinates": { "type": "Point", "coordinates": [35.9106, 31.9539] }
}
```

#### Success response — `200 OK`

```json
{
  "customer": { "_id": "665f1c9d2c8e4d0012345681" },
  "message": "Saved address added successfully"
}
```

#### Errors

- `400` — missing customer ID or location data.
- `401` — missing or invalid JWT.
- `403` — token is not a customer token.
- `500` — duplicate address, validation, customer, or database error.

### Save a restaurant

`POST /api/v1/customer/saved-restaurant`

**Access:** Customer JWT

#### Request body

```json
{ "restaurantId": "665f1c9d2c8e4d0012345678" }
```

#### Success response — `200 OK`

```json
{ "message": "restaurant added to saved successfully" }
```

#### Errors

- `400` — missing customer or restaurant ID.
- `401` — missing or invalid JWT.
- `403` — token is not a customer token.
- `500` — customer/restaurant not found, duplicate saved restaurant, or database error.

### List saved restaurants

`GET /api/v1/customer/saved-restaurant`

**Access:** Customer JWT

#### Success response — `200 OK`

```json
{
  "savedRestaurants": [],
  "message": "Saved restaurants fetched successfully"
}
```

Populated restaurant fields are currently requested as `name`, `logo`, `coverImage`, and `isOpen`.

### Cancel customer order

`POST /api/v1/customer/orders/:orderId/cancel`

**Access:** Customer JWT

#### Success response — `200 OK`

```json
{ "message": "Order cancelled successfully" }
```

Only orders with `pending` status are changed. For another current status, the endpoint returns `200` with:

```json
{ "message": "cannot cancel order" }
```

#### Errors

- `400` — missing customer or order ID.
- `401` — missing or invalid JWT.
- `403` — token is not a customer token.
- `500` — customer/order not found or order already cancelled.

## Cart endpoints

Base path: `/api/v1/cart`

### Create cart

`POST /api/v1/cart`

**Access:** Authenticated JWT. The route currently does not enforce a customer role.

The customer ID is taken from the JWT; no request body is required.

#### Success response — `201 Created`

The raw cart document is returned:

```json
{
  "_id": "665f1c9d2c8e4d0012345682",
  "customerId": "665f1c9d2c8e4d0012345681",
  "items": [],
  "totalPrice": 0
}
```

#### Errors

- `400` — customer ID missing from the JWT.
- `401` — missing or invalid JWT.
- `500` — cart already exists or database error.

### Get cart

`GET /api/v1/cart`

**Access:** Authenticated JWT. The route currently does not enforce a customer role.

#### Success response — `200 OK`

Returns the raw cart document for the JWT subject.

#### Errors

- `400` — customer ID missing from the JWT.
- `401` — missing or invalid JWT.
- `500` — cart does not exist or database error.

### Add cart item

`POST /api/v1/cart/items`

**Access:** Customer JWT

#### Request body

```json
{
  "productId": "665f1c9d2c8e4d0012345679",
  "quantity": 2,
  "variant": { "size": "regular", "price": 3.5 }
}
```

| Field       | Type   |             Required             |
| ----------- | ------ | :------------------------------: |
| `productId` | string |               Yes                |
| `quantity`  | number |               Yes                |
| `variant`   | object | Yes; includes `size` and `price` |

#### Success response — `200 OK`

```json
{ "message": "Item added to cart successfully" }
```

#### Errors

- `400` — `{ "message": "Product ID, quantity, and variant are required" }`
- `401` — missing or invalid JWT.
- `403` — token is not a customer token.
- `500` — cart missing, item already exists, invalid item, or database error.

### Delete cart item

`DELETE /api/v1/cart/items`

**Access:** Customer JWT

#### Request body

```json
{ "productId": "665f1c9d2c8e4d0012345679" }
```

#### Success response — `200 OK`

```json
{ "message": "Item deleted from cart successfully" }
```

#### Errors

- `400` — product ID is required.
- `401` — missing or invalid JWT.
- `403` — token is not a customer token.
- `500` — cart or item does not exist.

### Clear cart

`DELETE /api/v1/cart/clear`

**Access:** Customer JWT

#### Success response — `200 OK`

```json
{ "message": "Cart cleared successfully" }
```

#### Errors

- `401` — missing or invalid JWT.
- `403` — token is not a customer token.
- `500` — cart does not exist or database error.

## Order endpoints

Base path: `/api/v1/orders`

### Create order

`POST /api/v1/orders`

**Access:** Customer JWT

#### Request body

```json
{
  "customerId": "665f1c9d2c8e4d0012345681",
  "restaurantId": "665f1c9d2c8e4d0012345678",
  "deliveryDetails": {
    "type": "delivery",
    "location": {
      "city": "Amman",
      "coordinates": { "type": "Point", "coordinates": [35.9106, 31.9539] }
    }
  }
}
```

| Field                                  | Type                   |           Required            |
| -------------------------------------- | ---------------------- | :---------------------------: |
| `customerId`                           | string                 |              Yes              |
| `restaurantId`                         | string                 |              Yes              |
| `deliveryDetails.type`                 | `delivery` or `pickup` |              Yes              |
| `deliveryDetails.location`             | object                 | Yes in the current validator  |
| `deliveryDetails.location.city`        | string                 |              No               |
| `deliveryDetails.location.coordinates` | GeoJSON Point          | Yes when location is supplied |

The server reads the customer's cart, calculates the subtotal, applies a delivery fee of `200` for delivery and `0` for pickup, creates a pending order, and clears the cart in a transaction.

#### Success response — `201 Created`

```json
{
  "message": "Order created successfully",
  "order": {
    "orderNumber": "JIBLI-ORD-A1B2C3",
    "customerId": "665f1c9d2c8e4d0012345681",
    "restaurantId": "665f1c9d2c8e4d0012345678",
    "items": [],
    "subtotal": 7,
    "totalPrice": 207,
    "status": "pending",
    "deliveryDetails": {
      "fee": 200,
      "type": "delivery"
    }
  }
}
```

#### Errors

- `400` — `{ "message": "Order data is required" }` when the body is absent.
- `401` — missing or invalid JWT.
- `403` — token is not a customer token.
- `500` — restaurant not found/not open, validation, missing cart, empty cart, transaction, or database error.

### Get order by ID

`GET /api/v1/orders/:orderId`

**Access:** Customer JWT

#### Success response — `200 OK`

```json
{
  "message": "Order retrieved successfully",
  "order": {
    "_id": "665f1c9d2c8e4d0012345683",
    "orderNumber": "JIBLI-ORD-A1B2C3",
    "status": "pending"
  }
}
```

The repository scopes the query to both `orderId` and the authenticated customer ID.

#### Errors

- `400` — order ID or customer ID is missing.
- `401` — missing or invalid JWT.
- `403` — token is not a customer token.
- `500` — order not found or database error.

### List customer orders

`GET /api/v1/orders`

**Access:** Customer JWT

#### Success response — `200 OK`

```json
{
  "message": "Orders retrieved successfully",
  "orders": []
}
```

Orders are filtered by the authenticated customer's ID. Pagination and sorting parameters are not currently implemented.

#### Errors

- `400` — customer ID is missing from the JWT.
- `401` — missing or invalid JWT.
- `403` — token is not a customer token.
- `500` — database error.

## Static uploads

`GET /uploads/<path>`

The application exposes the local `uploads` directory as static content. This is not an API JSON endpoint. Images created by the current restaurant/product flows are uploaded to Cloudinary rather than this directory.

## HTTP status codes and errors

The controllers explicitly use these statuses:

| Status | Meaning                                                                                                 |
| -----: | ------------------------------------------------------------------------------------------------------- |
|  `200` | Successful read or update.                                                                              |
|  `201` | Successful creation.                                                                                    |
|  `400` | Missing or immediately invalid request data.                                                            |
|  `401` | Missing authentication or authentication failure.                                                       |
|  `403` | Authenticated user lacks the required role or ownership.                                                |
|  `404` | Resource not found in ownership middleware.                                                             |
|  `429` | OTP rate limit exceeded.                                                                                |
|  `500` | Service, provider, validation, MongoDB, or uncaught application error under the current implementation. |

Application errors are handled globally by the `ApiError` middleware and use this shape:

```json
{
  "status": "error",
  "message": "Resource not found"
}
```

Known domain failures now use typed statuses, including `400` for invalid input or OTP failures, `401` for authentication failures, `403` for authorization failures, `404` for missing resources, `409` for conflicts, `429` for rate limits, and `503` when the SMS provider rejects a request. Unexpected errors are sanitized as `500 Internal Server Error`.

## Implementation notes

These points are important for consumers and maintainers of the current API:

1. **OTP resend behavior:** creating a new OTP deletes existing OTP records for the phone number. OTPs are bcrypt-hashed in the database and are sent only through the SMS provider.
2. **OTP reuse:** verification marks an OTP as checked, but the repository currently does not reject an already checked OTP. Treat OTPs as intended to be single-use and plan to fix this server-side.
3. **OTP attempts:** failed attempts are tracked and an OTP is deleted after the attempt threshold is reached. Concurrent verification is not currently atomic.
4. **Role `rider`:** accepted by OTP validation but not implemented by the service.
5. **Customer ownership on order creation:** the current order flow accepts `customerId` from the request body; clients should send the authenticated customer's ID. The server should ideally derive this value from the JWT.
6. **Cart role checks:** cart creation and retrieval require a JWT but currently do not require the customer role.
7. **Product ownership on creation:** the product creation request includes `restaurantId`; clients should send the authenticated restaurant's ID.
8. **Product update variants:** product creation expects JSON-encoded `variants` in multipart data. Update handling does not currently parse `variants` in the same way.
9. **Search input:** restaurant name search is case-insensitive. Clients should URL-encode the query value.
10. **No pagination:** list endpoints currently return all matching records.
11. **Global error middleware:** `ApiError` responses are normalized to `{ status: "error", message }`. Mongoose validation, cast, duplicate-key, and JWT errors are also mapped to safe HTTP responses.
12. **No OpenAPI document is currently generated:** this Markdown file is the human-readable contract; it should be updated whenever routes, validators, or response shapes change.
